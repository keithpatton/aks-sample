name: $(Date:yyyyMMdd)$(Rev:.r)

trigger: none

resources:
- repo: self

variables: 
  - template: Templates/Variables/root.yml   

stages:

- template: Templates/common-stage.yml
  parameters:
    svcConnAzureRm: $(svcConnAzureRmDefault)
    dependsOn: []

- template: Templates/reg-env-stage.yml
  parameters:
    svcConnAzureRm: $(svcConnAzureRmDefault)
    region: 'au1'
    env: 'dev'
    dependsOn: [common]

- template: Templates/reg-env-stage.yml
  parameters:
    svcConnAzureRm: $(svcConnAzureRmDefault)
    region: 'au1'
    env: 'qa'
    dependsOn: [common]